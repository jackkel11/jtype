<Window x:Class="JType.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="J-Type Word Processor" Height="700" Width="1000">
    <DockPanel>
        <!-- Menu Bar -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="New Tab" Click="NewTab_Click"/>
                <MenuItem Header="Open" Click="Open_Click"/>
                <MenuItem Header="Save" Click="Save_Click"/>
                <Separator/>
                <MenuItem Header="Exit" Click="Exit_Click"/>
            </MenuItem>

            <MenuItem Header="_Edit">
                <MenuItem Header="Undo" Click="Undo_Click"/>
                <MenuItem Header="Redo" Click="Redo_Click"/>
                <Separator/>
                <MenuItem Header="Cut" Command="ApplicationCommands.Cut"/>
                <MenuItem Header="Copy" Command="ApplicationCommands.Copy"/>
                <MenuItem Header="Paste" Command="ApplicationCommands.Paste"/>
                <Separator/>
                <MenuItem Header="Search / Replace" Click="SearchReplace_Click"/>
            </MenuItem>
        </Menu>

        <!-- Toolbar -->
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Content="B" FontWeight="Bold" Click="Bold_Click"/>
                <Button Content="I" FontStyle="Italic" Click="Italic_Click"/>
                <Button Click="Underline_Click">
                    <TextBlock Text="U" TextDecorations="Underline"/>
                </Button>
                <Separator/>
                <ComboBox x:Name="FontFamilyBox" Width="150" SelectionChanged="FontFamilyBox_SelectionChanged"/>
                <ComboBox x:Name="FontSizeBox" Width="60" SelectionChanged="FontSizeBox_SelectionChanged"/>
                <Separator/>
                <xctk:ColorPicker x:Name="TextColorPicker" Width="120"
                                  SelectedColorChanged="TextColorPicker_SelectedColorChanged"/>
                <xctk:ColorPicker x:Name="HighlightColorPicker" Width="120"
                                  SelectedColorChanged="HighlightColorPicker_SelectedColorChanged"/>
                <Separator/>
                <ComboBox x:Name="LineSpacingBox" Width="80" SelectionChanged="LineSpacingBox_SelectionChanged">
                    <ComboBoxItem Content="1" />
                    <ComboBoxItem Content="1.15" />
                    <ComboBoxItem Content="1.5" />
                    <ComboBoxItem Content="2" />
                </ComboBox>
                <Separator/>
                <Button Content="Undo" Click="Undo_Click"/>
                <Button Content="Redo" Click="Redo_Click"/>
            </ToolBar>
        </ToolBarTray>

        <!-- Tab Control -->
        <TabControl x:Name="TabEditor" DockPanel.Dock="Bottom">
            <TabItem Header="Document 1">
                <RichTextBox AcceptsTab="True" VerticalScrollBarVisibility="Auto"
                             SpellCheck.IsEnabled="True" TextChanged="RichTextBox_TextChanged"/>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
